Cheon and Hwang conjectured in 1992 that E_k(r) + E_k(c) - P_k(A) <= 2 - k!/n^k
for every non-negative n x n matrix of total sum n and every 1 <= k <= n, with
equality only at J_n/n; the endpoint k = n is Dittert's conjecture, and the
intermediate range 2 < k < n appears to have gone untouched. One structural
fact organises what follows: the deficit has degree k, not n. Where k is small
enough that a fixed-degree Positivstellensatz ansatz still matches it, the
symmetry-reduced programme has the same 12 x 19 shape at every dimension and is
solved once over Q(n); where it is not, only inequalities uniform in n reach,
and a local method takes over.

On the first route we settle the line k = 3 for every n >= 4 -- the inequality,
the equality case, and a quantitative strengthening

    (2 - 6/n^3) - Phi_3(A)  >=  theta_2(n) ||A - J_n/n||_F^2,
    theta_2(n) = (n^4 + 40 n^2 - 84 n + 40) / (n^5 (n-1)^3 (n-2)),

by a single certificate whose nineteen coefficients are rational functions of
n, its two n^2 x n^2 Gram matrices reduced in closed form to ten rational
functions decided by Sturm sequences. This is machine-checked end to end
(subDittert_k3_full).

The second route is local: confinement traps any violator in a collar of the
Birkhoff polytope, a collar matrix splits orthogonally into a line-sum block
and a doubly centred block, the centred block is governed by a stability form
of the Tverberg-Friedland theorem proved here, and the five cross terms between
the blocks have exact reductions. That yields the Cheon-Hwang inequality at
k = 4 for every n >= 10 with its equality case. The stability theorem -- that
sigma_k on the doubly stochastic polytope exceeds its minimum by at least
C(n,k)^2 c(n,k) ||A - J_n/n||_F^2, with c(n,k) = k(k-1)k!/(4 n^k (n-1)^2)
optimal to within a factor two -- is kernel-checked at k = 2, k = 3 and k = 4
over the whole range it states for each, and is of independent interest.
Fixed-dimension certificates settle (k=4, n=5,6,7), so on the line k = 4
exactly two cells remain open, n = 8 and n = 9. Every result is stated with the
layer it rests on -- kernel-checked in Lean 4 at a pinned commit, exact
rational verifier, or verified computation -- and the grading is part of the
claim.

[NOTE ON THIS TEXT: reproduced from paper_b.typ's abstract block, modulo the
plain-text rendering of the mathematics.

Updated 2026-07-30 for the FULL REWRITE that cut the paper from 38 pages to 12.
What changed, and what did not:

- The paper now carries one thesis -- deg F = k, not n -- from which the map of
  methods follows: a certificate uniform in n where the fixed-degree ansatz
  still matches (k = 3); the local method where only n-uniform inequalities
  reach (k = 4, large n); exact per-cell certificates where n is small
  (k = 4, n = 5, 6, 7). The stability theorem is the local method's engine.
- Theorem letters A-H are UNCHANGED, as are Corollary B, Lemmas S1-S3 and
  Propositions S4-S6, and every Lean declaration name. They are external
  anchors. Proposition S7 (the rank-one D(u)D(v) analysis) was dropped to the
  kit; the conditional (epsilon <= 1/16) variant is now one clause naming it as
  a parked research problem, not a boxed statement.
- The sensitivity table MOVED OUT of the paper body to kit/sensitivity-k4.md,
  and graded_verify_k4.py was re-pointed at that file in lockstep. The
  exactly-ten-rows check, the per-row exact recomputation and the
  band-flat-at-10 assertion all survive unchanged, as does control M4. The
  paper cites the computation in one sentence.
- graded_verify_stability.py is UNCHANGED. Its V12 still parses the paper's
  displayed audit counts -- (33,33), (14,14), (46,46), (28,28), total 121 --
  and reads the Lean sources at the pinned commits 1507013, 27bda3f, 944b517,
  365e44e.
- The honest limits survive verbatim in substance: threshold 8 in Theorem G is
  a limit of the argument and NOT known sharp, with (k = 4, 4 <= n <= 7)
  undecided there; the gap is stated ONCE -- (k = 4, n = 8) has five of the six
  checks, the assembled 2144x2144 factorisation having not run, and
  (k = 4, n = 9) has no witness at all; the (7,4) join check is described as a
  consistency check between stored artefacts, not an independent verification.
- What was cut is process narration, per-cell fold history, duplicate gap
  statements, and the log-by-log (7,4) narrative (now three sentences, with
  filenames in the kit rather than in body prose).
- The DITTERT-PROGRESS STORY gets no chapter and barely a mention: the k = n
  public-claims table is gone, the verbatim README quote is gone, and the whole
  topic -- broader Dittert context, campaign narrative and competitor
  positioning together -- is ONE sentence in section 1.4: the endpoint is not
  attacked here and its status is not this paper's subject, one public
  unrefereed repository claims Dittert in full on which we take no view, and
  every priority claim is narrowed to what Lean and the verifiers carry.
  Dittert appears otherwise only to define the object (the conjecture
  statement, the k = n scope line, and Hwang's n = 3 theorem in Corollary B).
- TYPOGRAPHY: journal-grade styling. One statement style throughout (a left
  rule, heavier for named theorems, no fills or rounded corners); a proper
  title block with a centred rule; italic run-in heads and italic emphasis
  instead of boldface (bold survives only on statement heads and table
  headers, 20 spans down from about 130); booktabs tables, horizontal rules
  only; italic level-2 headings. Inline raw is sized RELATIVELY at 0.88em, in
  Nimbus Mono PS, so a declaration name scales with whatever context it sits
  in and sits slightly smaller than the surrounding prose; the tinted code
  boxes are gone. Equation cross-references render as (19), not "Equation 19".
  The space freed by the Dittert cut went to breathing room -- leading 0.53em
  to 0.58em, more air around headings, statements and displays -- not to new
  content.

Both verifiers pass against the finished paper, both --mutate runs fire every
control, and the process-term scan is clean.]
